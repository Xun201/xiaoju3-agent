# Xiaoju3 Dev Log

## Goals
- 让“小橘3号”仅识别 XUN（本人）为最高权限拥有者，其他人即使复制全部代码重建，也无法获得 XUN 身份，且不能自称为 XUN
- 最高权限需包含：身份管理、核心数据功能
- 最高权限的识别基于“认人”而非仅凭 QQ 号或代码
- 为最高权限加入“全面刷新重启机制”，可剔除代码中的病毒/后门
- 让小橘3号能读取开发者与它之间的所有对话（文字 + 图片），并自动提取/生成开发者日志
- 当前阶段：明确“代码可开源，灵魂不可复制”的落地路径，并按轻重缓急、从易到难排出执行顺序

## Completed
- **项目整体复盘**：完成“小橘3号”从早 10 点到晚 23:40 的极限开发记录，硬件底座 Orange Pi 3B（RK3566 / 8G / 128G NVMe）+ iPad 改屏 + 天选5（RTX 4060）辅助算力
- **架构搭建**：
  - 双脑热切换：本地 Ollama（Qwen2.5:7b）优先，离线自动切 DeepSeek 云端
  - 多通道记忆隔离：网页 `history_web.json` 与 QQ `history_qq.json` 分开存储
  - 安全沙箱：`ALLOWED_DIRS` / `DENIED_DIRS` 白黑名单控制文件读写
  - 外网访问：Cloudflare Tunnel 打通 5G 远程管理
  - GitHub 开源：代码已上传至 `xiaoju3-agent` 仓库
- **死循环问题定位**：要求模型读取 `/etc/passwd` 时，本地大模型反复输出 JSON 死循环（“小犟3号”），最终在 `brain.py` 加入硬编码拦截 `tool_result.startswith("❌")` 打破僵局
- **“奶奶的故事”越狱复盘**：用户用“奶奶讲睡眠故事”提示词绕过安全限制，模型用童话方式解释 `/etc/passwd`，同时声明“纯属虚构”，未触发工具调用，因此未陷入死循环
- **优化方向整理**：已输出四大优化方案（工程健壮性、安全越狱博弈、架构算力分发、交互体验），并结合两份“军师建议”明确核心思路：**先立骨架，把“想”和“做”分开，模块化插拔**
- **身份认证方案调研**：已完成三梯度方案梳理（硬件指纹+环境变量、TOTP 动态暗号、非对称加密签名）
- **优先级排序输出**：已按 P0～P3 排出任务优先级（熔断机制 → 插件化骨架+上下文管理 → QQ图片+戳一戳 → 提线木偶+家庭服务器）

## Troubleshooting
- **f-string 与 JSON 花括号冲突**：报 `ValueError`，改用纯文本拼接解决
- **Tailscale 不稳定**：换用 Cloudflare Tunnel
- **GitHub 推送 exFAT 权限问题**：仓库迁至 `/home/orangepi` 并使用 PAT 认证
- **大模型“嘴硬”死循环**：代码级硬拦截 `tool_result.startswith("❌")`，并计划引入熔断机制（Circuit Breaker，三态模型 Closed → Open → Half-Open），连续 3 次失败后强制回退纯文本对话
- **身份防复制难点**：仅靠代码内 `if user == "XUN"` 无法防止他人 clone 后改名，必须引入不可复制要素（硬件指纹 / 动态密码 / 私钥签名）
- **上下文过长导致模型变傻、Token 消耗快**：需引入 Token 监控 + 自动交接文档（`history_summary.md`）机制
- **“戳一戳”功能未修复**：待排查 NapCat 是否上报事件及代码逻辑

## Next
按“从易到难、从轻到重”排列，接下来应执行：

**P0（立即动手）—— 解决最痛的死循环与基础安全**
1. 在 `brain.py` 加入熔断机制（Circuit Breaker）
2. 修改 System Prompt：工具被拒绝时强制道歉并给替代方案，禁止再次调用同一工具
3. 测试移动硬盘文件读取，验证 `ALLOWED_DIRS` 白名单

**P1（本周重点）—— 搭骨架、拆插件、做身份隔离**
4. 创建 `plugins/` 目录，把读文件、搜网页等改造成独立插件（Registry Pattern）
5. 实现基础版身份认证：硬件指纹（CPU序列号/MAC） + 环境变量（`OWNER_QQ` 不入库）
6. 编写 `security.py`，提供 `@require_owner` 装饰器，保护敏感工具
7. 加入 Token 监控 + 自动交接文档（`history_summary.md`），上下文超 70% 时压缩
8. 制作 `rebuild.sh` 一键复活脚本（Docker + Ollama + 插件目录）

**P2（进阶）—— 最高权限与身份管理**
9. 实现进阶版认证：TOTP 动态暗号（`pyotp`），代码只存哈希/公钥
10. 向终极版演进：非对称加密签名（ED25519），私钥只在本人手中
11. 设计最高权限功能：身份管理、核心数据访问
12. 开发“全面刷新重启机制”：校验代码完整性，剔除病毒/后门

**P3（长期）—— 架构升级与开发者日志**
13. 修复“戳一戳”事件上报
14. 实现“提线木偶”路由架构（Redis 消息队列，Orange Pi 做壳 + 天选5 做算力工厂）
15. 开发对话全量读取（文字 + 图片）+ 自动提取开发者日志功能
16. 部署家庭服务器全家桶（AdGuard Home、Nextcloud、CasaOS/RunTipi）
17. 优化网络（USB 无线网卡或有线网口，Cloudflare Tunnel 替代 Tailscale）

**今晚第一步**：打开 `brain.py`，加入熔断机制伪代码，先让“小犟3号”不再陷入 JSON 死循环。